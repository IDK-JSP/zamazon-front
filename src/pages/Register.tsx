import {FC} from 'react';
import RegisterForm from "../component/RegisterFrom";

const Register: FC<{}> = ({}) => {
    return (
        <div>
            <RegisterForm/>
        </div>
    );
};

export default Register;
